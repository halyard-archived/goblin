#!/usr/bin/bash

codedir="<%= @codedir %>"
envdir="<%= @envdir %>"
modulepath="<%= @modulepath %>"
manifestpath="<%= @manifestpath %>"

cd $codedir
puppet apply --modulepath $modulepath $manifestpath

