#!/usr/bin/bash

codedir="<%= /etc/puppetlabs/code"
envdir="${codedir}/environments/production"
modulepath="${envdir}/modules:${codedir}/modules"
manifestpath="${envdir}/manifests/site.pp"

cd $codedir
puppet apply --modulepath $modulepath $manifestpath

